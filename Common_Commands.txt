#To initiatialize a git object:
git init

#For a new repo (link from "Code" green button)
git remote add origin https://github.com/XXXX/XXXXX/git 

#To add new objects to existing repo:
git add .

#To commit changes with description "Bullbitch":
git commit -m "BullBitch"

#To push changes (specific for me, the changes are pushed to the master branch instead of the main branch)
git push origin HEAD
